<?xml version="1.0" encoding="utf-8"?>
<translate xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="1000"
    android:fromXDelta="-100%"
    android:toXDelta="100%"
    android:fillBefore="true"
    android:fillAfter="true"
    android:repeatCount="4"
    android:startOffset="100"
    android:repeatMode="reverse"
    android:interpolator="@android:anim/accelerate_decelerate_interpolator" />

    <!--android:duration：动画持续时长-->
    <!--android:fillAfter：动画结束之后是否保持动画的最终状态；true，表示保持动画的最终状态-->
    <!--android:fillBefore：动画结束之后是否保持动画开始前的状态；true，表示恢复到动画开始前的状态-->

    <!--android:fromXDelta：动画开始时，在X轴方向上的位置；取值类型有三种：数字；百分比；百分比+”p”;-->
    <!--数字：例如50.0，这里的单位是px像素-->
    <!--百分比：例如50%，这里是相对于自己控件宽度的百分比，实际的值是mIvImg.getWidth()*50%；-->
    <!--百分比+”p”：例如50%p，这里是表示相对于自己控件的父控件的百分比，-->

    <!--android:toXDelta：动画移动在X轴的目标位置；取值类型和fromXDelta一样-->

    <!--android:repeatCount：动画重复的次数。指定动画重复播放的次数，默认0不重复,如果你需要无限循环播放，请填写一个小于0的数值，我一般写-1-->

    <!--android:repeatMode：动画重复的Mode，有reverse(已结束为起点)和restart（以最开始未起点）两种，效果看后面-->

    <!--android:startOffset：动画播放延迟时长，就是调用start之后延迟多少时间播放动画 单位毫秒-->



    <!--android:interpolator="@android:anim/accelerate_decelerate_interpolator" #插值器，后面单独讲 加速 匀速  减速运行
    linear_interpolator为匀速效果，accelerate_decelerate_interpolator为加速效果、decelerate_interpolator为减速效果，-->